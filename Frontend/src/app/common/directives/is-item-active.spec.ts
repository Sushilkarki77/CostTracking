import { IsItemActive } from './is-item-active';

describe('IsItemActive', () => {
  it('should create an instance', () => {
    const directive = new IsItemActive();
    expect(directive).toBeTruthy();
  });
});
